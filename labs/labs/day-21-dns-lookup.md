# Day 21 - DNS Lookup Lab (dig, nslookup, host)

## Goal
Understand how DNS resolution works using Linux tools.

---

## Tools Used
- `nslookup`
- `dig`
- `host`

Install tools if missing:
```bash
sudo apt update
sudo apt install dnsutils -y
